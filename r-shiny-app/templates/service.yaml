apiVersion: v1
kind: Service
metadata:
  name: {{ include "r-shiny-app.fullname" . }}
  labels:
    {{- include "r-shiny-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "r-shiny-app.selectorLabels" . | nindent 4 }}